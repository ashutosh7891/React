import React from "react";

// part one => we are getting a method from React known as createContext and storing inside a variable

const userContext = React.createContext()
 

export default userContext;